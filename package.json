{
  "name": "ragnarok",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd client && yarn install",
    "start": "foreman start -f Procfile.dev",
    "test": "rspec && cd client && yarn test",
    "test:watch": "cd client && yarn test:watch",
    "test:ci": "cd client && yarn test:ci",
    "eslint": "cd client && yarn eslint"
  }
}
